{
  "name": "@headless/types",
  "version": "1.0.0",
  "description": "typings",
  "main": "types.ts",
  "module": "types.ts",
  "scripts": {
    "audit": "eslint && tsc -p tsconfig.json",
    "build": "npm run build:dts",
    "build:dts": "tsc types.ts --declaration --emitDeclarationOnly --outDir dist",
    "test": "echo \"placeholder\" && exit 0",
    "clean": "rimraf dist doc *.log *.tgz node_modules/.*",
    "doc": "typedoc types.ts"
  },
  "author": "jesse23",
  "license": "MIT"
}
