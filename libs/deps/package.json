{
  "name": "@headless/deps",
  "version": "1.0.0",
  "description": "deps lib example",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "audit": "eslint src test --ext .js,.jsx,.ts,.tsx && tsc",
    "build": "tsx esbuild",
    "test": "vitest run",
    "clean": "rimraf dist doc *.log *.tgz node_modules/.*"
  },
  "author": "jesse23",
  "license": "ISC",
  "devDependencies": {
    "date-fns": "^2.30.0"
  }
}
