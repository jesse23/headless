{
  "name": "@headless/utils",
  "version": "1.0.0",
  "description": "Internal utilities for mono repo",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "audit": "eslint src test && tsc",
    "build": "echo \"placeholder\" && exit 0",
    "test": "vitest run",
    "clean": "rimraf dist doc *.log *.tgz node_modules"
  },
  "dependencies": {
    "@headless/types": "^1.0.0"
  },
  "devDependencies": {
    "@headless/vitest": "^1.0.0"
  },
  "author": "jesse23",
  "license": "MIT"
}
