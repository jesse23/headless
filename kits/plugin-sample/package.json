{
  "name": "@headless/plugin-sample",
  "version": "1.0.0",
  "description": "Kit sample loaded at runtime",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "audit": "eslint src test && tsc",
    "build": "tsx esbuild",
    "test": "vitest run",
    "clean": "rimraf dist doc *.log *.tgz node_modules"
  },
  "peerDependencies": {
    "@headless/core": "^1.0.0"
  },
  "devDependencies": {
    "@headless/utils": "^1.0.0",
    "@headless/types": "^1.0.0",
    "@headless/esbuild": "^1.0.0",
    "@headless/vitest": "^1.0.0"
  },
  "author": "jesse23",
  "license": "MIT"
}
