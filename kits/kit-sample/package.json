{
  "name": "@headless/kit-sample",
  "version": "1.0.0",
  "description": "sample kits that will be built with app",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "audit": "eslint src test && tsc",
    "build": "tsx esbuild",
    "test": "vitest run",
    "clean": "rimraf dist doc *.log *.tgz node_modules"
  },
  "peerDependencies": {
    "@headless/core": "^1.0.0",
    "@headless/interop": "^1.0.0"
  },
  "devDependencies": {
    "@headless/types": "^1.0.0",
    "@headless/esbuild": "^1.0.0",
    "@headless/vitest": "^1.0.0"
  },
  "author": "jesse23",
  "license": "MIT"
}
